<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/main.css" />
    <title>Projects</title>
</head>
<body>
    <%-include('partials/navbar',{page: '/solutions/projects'})%>

       <div class="hero bg-base-200 p-4 w-full"> <!--This image should be royalty free :D-->
        <div class="hero-content text-center">
          <div class="max-w-md">
            <h1 class="text-5xl font-bold">Our Projects!</h1>
            <p class="py-6">
                Big projects from our minds to your screens. <br> Take a look at some of our industries we already work with!
            </p>
            <button class="btn btn-primary" onclick="location.href='/solutions/projects?sector=Industry'";>Industry</button>
            <button class="btn btn-primary" onclick="location.href='/solutions/projects?sector=Transportation'";>Transportation</button>
            <button class="btn btn-primary" onclick="location.href='/solutions/projects?sector=Electricity'";>Electricity</button>
          </div>
        </div>
      </div>
    
      <div className="overflow-x-auto">
  <table className="table">
    <thead>
      <tr>
        <th>Title</th>
        <th>Sector</th>
        <th>Summary</th>
        <th>Details</th>
      </tr>
    </thead>
    <tbody>
        <%projects.forEach(project=>{ %>
            <tr>
              <td><%= project.title%></td>
              <td><button class="btn btn-wide btn-xs sm:btn-sm md:btn-md lg:btn-lg xl:btn-xl" onclick="location.href='/solutions/projects?sector=<%=project.sector%>'"><%= project.sector%></button></td>
              <td><%= project.summary_short%></td>
              <td><button class="btn btn-xs sm:btn-sm md:btn-md lg:btn-lg xl:btn-xl" onclick="location.href='/solutions/projects/<%=project.id%>'">Details</button></td>
            </tr>
        <%})%>
    </tbody>
  </table>
</div>
</body>
</html>